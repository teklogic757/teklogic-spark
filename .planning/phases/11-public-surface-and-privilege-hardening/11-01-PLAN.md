---
phase: 11-public-surface-and-privilege-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - middleware.ts
  - src/lib/security-headers.ts
  - src/lib/security-headers.test.ts
  - src/lib/supabase/server.ts
  - .planning/phases/11-public-surface-and-privilege-hardening/11-VALIDATION.md
autonomous: true
requirements:
  - DSEC-03
user_setup: []
must_haves:
  truths:
    - Public-facing responses carry an explicit, reviewable browser security header contract instead of ad hoc header mutations in middleware
    - Authentication cookie posture is explicit for public-internet deployment rather than inherited implicitly from library defaults
    - Header and cookie hardening remains testable without requiring full end-to-end browser automation
  artifacts:
    - path: src/lib/security-headers.ts
      provides: Centralized public response header and cookie policy that middleware and future routes can reuse
      min_lines: 80
    - path: middleware.ts
      provides: Public request pipeline wired to the shared security policy
      min_lines: 90
    - path: src/lib/security-headers.test.ts
      provides: Targeted coverage for the internet-facing header contract and HTTPS-sensitive branches
      min_lines: 100
  key_links:
    - from: middleware.ts
      to: src/lib/security-headers.ts
      via: middleware applies a shared header policy instead of open-coded response mutations
      pattern: security headers
    - from: src/lib/supabase/server.ts
      to: src/lib/security-headers.ts
      via: session cookie posture aligns with the same public-internet boundary as the response headers
      pattern: cookie policy
---

<objective>
Create the browser-facing hardening baseline for Phase 11 by centralizing response header and cookie policy.

Purpose: The current middleware sets a partial header set inline and relies on implicit auth-cookie defaults. Public deployment needs a single, reviewable contract for browser protections and session-cookie posture before redirect and admin hardening build on top of it.
Output: A shared security-header helper, explicit cookie policy decisions, and targeted tests for the public request boundary.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-public-surface-and-privilege-hardening/11-CONTEXT.md
@.planning/phases/11-public-surface-and-privilege-hardening/11-VALIDATION.md
@middleware.ts
@src/lib/supabase/server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract a shared public security header policy</name>
  <files>src/lib/security-headers.ts, middleware.ts</files>
  <action>Move the current inline middleware header logic into a dedicated helper that defines the minimum production-safe browser policy for this app. Keep the policy scoped to Phase 11: explicit header values, HTTPS-sensitive HSTS behavior, and anything needed to make future route hardening consistent rather than scattered.</action>
  <verify>npm run test -- --run src/lib/security-headers.test.ts</verify>
  <done>Public request handling has one authoritative security-header contract that is easy to review and reuse.</done>
</task>

<task type="auto">
  <name>Task 2: Make auth cookie posture explicit and verify the middleware boundary</name>
  <files>src/lib/supabase/server.ts, middleware.ts, src/lib/security-headers.test.ts, .planning/phases/11-public-surface-and-privilege-hardening/11-VALIDATION.md</files>
  <action>Review how Supabase session cookies are created in the server and middleware paths, then set the app's intended public cookie posture explicitly where the current code allows it. Add focused tests around HTTPS/non-HTTPS branches and update the validation contract only if the actual verification commands need to change.</action>
  <verify>npm run lint</verify>
  <done>Security headers and session-cookie expectations are explicit for public deployment and covered by committed tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test -- --run src/lib/security-headers.test.ts` passes
- [ ] `npm run lint` passes
- [ ] Middleware applies the committed header policy through a shared helper instead of open-coded mutations
- [ ] Cookie behavior decisions for public deployment are explicit in code rather than implied
</verification>

<success_criteria>

- All tasks completed
- DSEC-03 header and cookie boundary is explicit and reviewable
- Public request hardening is centralized and testable
- Future auth/admin plans can build on a stable browser-surface contract
</success_criteria>

<output>
After completion, create `.planning/phases/11-public-surface-and-privilege-hardening/11-01-SUMMARY.md`
</output>
