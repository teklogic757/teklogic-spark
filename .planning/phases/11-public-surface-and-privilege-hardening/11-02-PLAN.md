---
phase: 11-public-surface-and-privilege-hardening
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/app/auth/callback/route.ts
  - src/app/login/actions.ts
  - src/app/login/login-form.tsx
  - src/lib/site-url.ts
  - src/lib/auth-redirect.ts
  - src/lib/auth-redirect.test.ts
autonomous: true
requirements:
  - DSEC-03
user_setup: []
must_haves:
  truths:
    - The canonical site URL remains the trust boundary for auth callbacks and public redirect generation in production
    - Login and callback flows accept only safe in-app redirect targets by default, with any external escape hatches made explicit and allowlisted
    - Suspicious or malformed redirect inputs fail closed to a safe destination and leave audit-friendly logging behind
  artifacts:
    - path: src/lib/auth-redirect.ts
      provides: Shared redirect normalization and host/origin enforcement logic for login and callback flows
      min_lines: 120
    - path: src/app/auth/callback/route.ts
      provides: Server-side callback enforcement that uses the shared safe-redirect contract
      min_lines: 40
    - path: src/lib/auth-redirect.test.ts
      provides: Coverage for relative redirects, suspicious external redirects, and canonical-host fallback behavior
      min_lines: 120
  key_links:
    - from: src/app/auth/callback/route.ts
      to: src/lib/auth-redirect.ts
      via: callback redirects reuse a single safe target resolver instead of per-route normalization
      pattern: safe redirect
    - from: src/app/login/login-form.tsx
      to: src/lib/site-url.ts
      via: client login entrypoints stop treating the current browser origin as production authority
      pattern: canonical site URL
---

<objective>
Harden public auth entrypoints so callback and login redirects fail closed on the canonical host.

Purpose: Phase 10 introduced a canonical site URL, but the current login and callback flows still split trust between server helpers and the live browser origin. This plan makes redirect handling explicit, normalized, and production-safe.
Output: Shared redirect enforcement logic, updated login/callback call sites, and focused regression coverage for hostile or malformed redirect inputs.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-public-surface-and-privilege-hardening/11-CONTEXT.md
@.planning/phases/11-public-surface-and-privilege-hardening/11-VALIDATION.md
@src/lib/site-url.ts
@src/app/auth/callback/route.ts
@src/app/login/actions.ts
@src/app/login/login-form.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a shared safe-redirect helper for auth flows</name>
  <files>src/lib/auth-redirect.ts, src/lib/site-url.ts, src/app/auth/callback/route.ts</files>
  <action>Add a shared helper that resolves safe post-auth redirect targets using the canonical site URL, strict relative-path defaults, and an explicit allowlist model for any external destinations that are truly required. Update the auth callback route to use it for both success and error redirects, including logging-friendly fallback behavior.</action>
  <verify>npm run test -- --run src/lib/auth-redirect.test.ts</verify>
  <done>The server-side callback route has one explicit redirect policy that fails closed to a safe in-app destination.</done>
</task>

<task type="auto">
  <name>Task 2: Align login entrypoints to the canonical redirect contract</name>
  <files>src/app/login/login-form.tsx, src/app/login/actions.ts, src/lib/auth-redirect.ts, src/lib/auth-redirect.test.ts</files>
  <action>Refactor the global login flow so magic-link and password entrypoints no longer assume the live browser origin is trustworthy in production. Keep local development working, but route production redirect generation and next-parameter handling through the same canonical contract used by the server callback path.</action>
  <verify>npm run test:ci && npm run build</verify>
  <done>Login and callback flows share one redirect trust model that is safe for public-internet deployment.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test -- --run src/lib/auth-redirect.test.ts` passes
- [ ] `npm run test:ci && npm run build` passes
- [ ] Invalid or suspicious redirect inputs fall back to a safe internal destination
- [ ] Production auth redirects derive from the canonical site URL contract rather than the incoming host or browser origin alone
</verification>

<success_criteria>

- All tasks completed
- DSEC-03 redirect and callback trust behavior is explicit and production-safe
- Auth flows reject unsafe `next` values and mismatched hosts by default
- Shared redirect logic is covered by committed regression tests
</success_criteria>

<output>
After completion, create `.planning/phases/11-public-surface-and-privilege-hardening/11-02-SUMMARY.md`
</output>
