---
phase: 11-public-surface-and-privilege-hardening
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/app/admin/layout.tsx
  - src/app/admin/actions.ts
  - src/app/admin/actions.test.ts
  - src/lib/supabase/server.ts
  - docs/DEPLOYMENT_SECURITY_CHECKLIST.md
  - docs/privileged-access.md
autonomous: true
requirements:
  - DSEC-04
user_setup: []
must_haves:
  truths:
    - Public `/admin` access makes authorization failures explicit instead of hiding them behind a generic redirect
    - Every remaining service-role path has a narrow operational rationale and is documented for deployment review
    - Privileged mutations stay constrained to the minimum required call sites rather than spreading through tenant-facing code by convenience
  artifacts:
    - path: src/app/admin/layout.tsx
      provides: Hard-deny behavior for authenticated non-admin access to the admin surface
      min_lines: 40
    - path: src/app/admin/actions.test.ts
      provides: Regression coverage for core admin authorization boundaries
      min_lines: 100
    - path: docs/privileged-access.md
      provides: Inventory of `createAdminClient()` usage, rationale, and operator review notes for public deployment
      min_lines: 80
  key_links:
    - from: src/app/admin/layout.tsx
      to: src/app/admin/actions.ts
      via: page-level admin access and mutation-level admin access enforce the same privilege boundary
      pattern: super admin guard
    - from: docs/privileged-access.md
      to: src/lib/supabase/server.ts
      via: service-role helper documentation names the intended boundary and remaining approved call sites
      pattern: createAdminClient
---

<objective>
Constrain and document privileged access before public deployment widens the blast radius of existing admin and service-role behavior.

Purpose: The app currently exposes `/admin` on the same host and uses `createAdminClient()` across several server paths. Phase 11 needs a hard authorization posture for public admin routes plus an explicit inventory of privileged surfaces so go-live review does not rely on memory.
Output: Clear admin denial behavior, focused authorization tests, and a deployment-facing privilege inventory that explains each approved service-role use.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-public-surface-and-privilege-hardening/11-CONTEXT.md
@.planning/phases/11-public-surface-and-privilege-hardening/11-VALIDATION.md
@src/app/admin/layout.tsx
@src/app/admin/actions.ts
@src/lib/supabase/server.ts
@docs/DEPLOYMENT_SECURITY_CHECKLIST.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make public admin authorization failures explicit and testable</name>
  <files>src/app/admin/layout.tsx, src/app/admin/actions.ts, src/app/admin/actions.test.ts</files>
  <action>Change the public admin route posture so authenticated non-admin users receive an explicit unauthorized or not-found style outcome instead of a soft redirect to the app shell. Keep the existing super-admin flow intact, and add focused tests for route-level and action-level authorization behavior.</action>
  <verify>npm run test -- --run src/app/admin/actions.test.ts</verify>
  <done>Public admin access clearly distinguishes unauthenticated, unauthorized, and authorized states without widening admin capability.</done>
</task>

<task type="auto">
  <name>Task 2: Document and tighten approved service-role boundaries</name>
  <files>src/lib/supabase/server.ts, docs/DEPLOYMENT_SECURITY_CHECKLIST.md, docs/privileged-access.md</files>
  <action>Inventory every remaining `createAdminClient()` call site, confirm which ones are still justified for public deployment, and tighten or annotate the helper and docs accordingly. Update deployment-facing documentation so operators can review privileged surfaces intentionally during go-live instead of inferring them from code comments alone.</action>
  <verify>npm run lint</verify>
  <done>DSEC-04 has a concrete privilege inventory and the service-role boundary is explicitly documented for production review.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test -- --run src/app/admin/actions.test.ts` passes
- [ ] `npm run lint` passes
- [ ] Authenticated non-admin users do not receive a soft redirect when they hit `/admin`
- [ ] A committed privilege inventory documents every approved `createAdminClient()` usage and its rationale
</verification>

<success_criteria>

- All tasks completed
- DSEC-04 privileged surfaces are documented and intentionally constrained
- Public admin authorization failures are explicit
- Service-role usage is reviewable during deployment without code archaeology
</success_criteria>

<output>
After completion, create `.planning/phases/11-public-surface-and-privilege-hardening/11-03-SUMMARY.md`
</output>
