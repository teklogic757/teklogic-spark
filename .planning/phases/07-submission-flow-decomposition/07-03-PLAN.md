---
phase: 07-submission-flow-decomposition
plan: "03"
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - src/app/[client_id]/submit/actions.ts
  - src/app/[client_id]/m/submit/actions.ts
  - src/lib/submit-flow.ts
  - .planning/STATE.md
autonomous: true
requirements:
  - SUBM-01
  - SUBM-02
user_setup: []
must_haves:
  truths:
    - The final desktop and mobile server actions are thin route adapters that call shared submission helpers, handle route-specific redirects, and return explicit error states
    - Failure branches are separated enough that future tests can target preparation, persistence, and post-persist steps without running the entire server action every time
    - Project state is updated to reflect that Phase 7 planning is complete and execution is the next step
  artifacts:
    - path: src/app/[client_id]/submit/actions.ts
      provides: Desktop action reduced to route-specific coordination and redirect handling
      min_lines: 100
    - path: src/app/[client_id]/m/submit/actions.ts
      provides: Mobile action reduced to route-specific coordination and redirect handling
      min_lines: 80
    - path: src/lib/submit-flow.ts
      provides: Final shared helper contracts that expose explicit failure boundaries for later test coverage
      min_lines: 220
  key_links:
    - from: src/app/[client_id]/submit/actions.ts
      to: src/lib/submit-flow.ts
      via: desktop action becomes a thin route adapter over the shared pipeline
      pattern: submitIdeaFlow
    - from: src/app/[client_id]/m/submit/actions.ts
      to: src/lib/submit-flow.ts
      via: mobile action becomes a thin route adapter over the shared pipeline
      pattern: submitIdeaFlow
---

<objective>
Finish the decomposition by making both submit actions thin route adapters with explicit failure handling.

Purpose: After extraction and side-effect isolation, the final shape should make the route actions easy to reason about and easy to test later in Phase 9. This plan collapses any remaining orchestration into shared helpers, keeps only route-specific branching in the actions, and updates planning state so execution can proceed cleanly.
Output: Thin desktop and mobile actions with explicit route-level behavior only, plus planning state aligned for Phase 7 execution.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-submission-flow-decomposition/07-VALIDATION.md
@.planning/phases/07-submission-flow-decomposition/07-01-PLAN.md
@.planning/phases/07-submission-flow-decomposition/07-02-PLAN.md
@src/app/[client_id]/submit/actions.ts
@src/app/[client_id]/m/submit/actions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reduce actions to thin route adapters</name>
  <files>src/app/[client_id]/submit/actions.ts, src/app/[client_id]/m/submit/actions.ts, src/lib/submit-flow.ts</files>
  <action>Remove any remaining broad orchestration from the desktop and mobile server actions so they mainly gather route-specific inputs, call the shared submission pipeline, translate typed failures into returned form errors, and perform their distinct redirect/revalidate behavior only after a successful persistence outcome.</action>
  <verify>npm run lint</verify>
  <done>The route actions are thin adapters over the shared pipeline instead of mixed orchestration blocks.</done>
</task>

<task type="auto">
  <name>Task 2: Lock in explicit failure boundaries for future test coverage</name>
  <files>src/lib/submit-flow.ts, src/app/[client_id]/submit/actions.ts, src/app/[client_id]/m/submit/actions.ts, .planning/STATE.md</files>
  <action>Ensure the shared helper exports or internal structure make preparation, persistence, and post-persist outcomes independently observable for future automated tests, then update planning state to mark Phase 7 as planned and ready for execution. Keep the Phase 7 outcome focused on better decomposition, not on adding the Phase 9 test harness early.</action>
  <verify>npm run build</verify>
  <done>Failure boundaries are explicit enough to target later, and the project state cleanly points to Phase 7 execution.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes
- [ ] `npm run build` passes
- [ ] Code review confirms future tests can target preparation, persistence, and post-persist outcomes separately
</verification>

<success_criteria>

- All tasks completed
- Desktop and mobile actions are thin route adapters
- The shared submission pipeline exposes explicit failure boundaries
- Phase 7 is ready to execute immediately after plan review
</success_criteria>

<output>
After completion, create `.planning/phases/07-submission-flow-decomposition/07-03-SUMMARY.md`
</output>
