---
phase: 01-submission-ux-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/client-validation.ts
  - src/components/submit-idea-form.tsx
  - src/app/[client_id]/m/submit/page.tsx
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02
user_setup: []
must_haves:
  truths:
    - Desktop submit flow rejects invalid attachments immediately after file selection
    - Desktop submit flow shows attachment rule guidance, selected-file details, and both inline plus form-level file errors
    - Mobile submit flow enforces the same client-side field validation rules as the desktop form before submit
    - Redirecting idea submission shows a short processing status while duplicate submits are blocked
  artifacts:
    - path: src/lib/client-validation.ts
      provides: Shared client-side validation helpers for field and file rules
      min_lines: 25
      exports:
        - validateIdeaField
        - validateIdeaFile
    - path: src/components/submit-idea-form.tsx
      provides: Desktop submit flow with immediate file validation and lightweight pending messaging
      min_lines: 50
      contains: selected-file
    - path: src/app/[client_id]/m/submit/page.tsx
      provides: Mobile submit flow aligned with the shared client validation behavior
      min_lines: 40
  key_links:
    - from: src/components/submit-idea-form.tsx
      to: src/lib/client-validation.ts
      via: imported validation helpers
      pattern: validateIdea(Field|File)
    - from: src/app/[client_id]/m/submit/page.tsx
      to: src/lib/client-validation.ts
      via: imported validation helpers
      pattern: validateIdeaField
---

<objective>
Harden the employee and mobile idea submission flows with shared client-side validation and clearer attachment UX.

Purpose: Deliver the highest-impact part of Phase 1 by making submission errors visible before the request reaches the server while keeping the interaction lightweight.
Output: A shared validation helper plus upgraded desktop and mobile submit flows.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-submission-ux-hardening/01-CONTEXT.md
@.planning/phases/01-submission-ux-hardening/01-VALIDATION.md
@src/lib/validators.ts
@src/components/submit-idea-form.tsx
@src/app/[client_id]/submit/actions.ts
@src/app/[client_id]/m/submit/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared client-side validation helpers</name>
  <files>src/lib/client-validation.ts, src/lib/validators.ts</files>
  <action>Create a client-safe validation helper module that mirrors the existing server-side field and file rules without duplicating magic numbers in every component. Reuse exported constants from the validator layer where practical, and keep the helper focused on synchronous, UI-ready messages for field-level and file-selection feedback.</action>
  <verify>npm run lint</verify>
  <done>Desktop and mobile submit flows can import one helper source for field and file validation behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade the desktop submit form UX</name>
  <files>src/components/submit-idea-form.tsx</files>
  <action>Update the desktop submit form to validate fields as the user interacts, reject invalid attachments immediately on file selection, and render a compact rules checklist plus a selected-file card with filename, size, and remove or replace control. Keep attachment errors visible both under the file field and in the existing top-level error area. Preserve the lightweight pending pattern by keeping the form visible, blocking duplicate submits, and adding a short processing message during redirecting submit flows.</action>
  <verify>npm run build</verify>
  <done>The desktop submit form surfaces client validation before submit and matches the Phase 1 context decisions for file and pending states.</done>
</task>

<task type="auto">
  <name>Task 3: Bring the mobile submit flow into parity</name>
  <files>src/app/[client_id]/m/submit/page.tsx</files>
  <action>Apply the same field-level client validation rules and lightweight pending semantics to the mobile submit page, adapted to its smaller layout. Keep the UI scoped to the existing mobile design, but ensure required fields fail early and redirecting submits show a brief processing status instead of a bare spinner-only transition.</action>
  <verify>npm run build</verify>
  <done>Mobile submission behavior matches the validation contract closely enough that users get the same core feedback before the request is sent.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes
- [ ] `npm run build` passes
- [ ] Manual spot check confirms invalid files are rejected immediately and valid files render a selected-file card
</verification>

<success_criteria>

- All tasks completed
- Desktop submit flow enforces client-side field and file validation before submit
- Mobile submit flow mirrors the field validation and pending-state improvements
- No full-form loading overlay or input lock is introduced
</success_criteria>

<output>
After completion, create `.planning/phases/01-submission-ux-hardening/01-01-SUMMARY.md`
</output>
