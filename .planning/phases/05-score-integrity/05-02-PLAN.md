---
phase: 05-score-integrity
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/app/[client_id]/submit/actions.ts
  - src/app/[client_id]/m/submit/actions.ts
  - src/lib/ai-evaluator.ts
autonomous: true
requirements:
  - SCOR-01
  - SCOR-02
user_setup: []
must_haves:
  truths:
    - Desktop and mobile submission flows both persist the same canonical rubric-derived score field for new ideas
    - Point awards are calculated from the same canonical score value that is written to `ideas.ai_score`
    - Submission code does not maintain a second trusted score path that can drift from persisted rubric math
  artifacts:
    - path: src/app/[client_id]/submit/actions.ts
      provides: Primary submission flow that inserts canonical score data and awards points from the same value
      min_lines: 140
    - path: src/app/[client_id]/m/submit/actions.ts
      provides: Mobile submission flow kept in lockstep with the primary score contract
      min_lines: 100
    - path: src/lib/ai-evaluator.ts
      provides: Finalized evaluator payload contract consumed consistently by both submit actions
      min_lines: 120
  key_links:
    - from: src/app/[client_id]/submit/actions.ts
      to: src/lib/ai-evaluator.ts
      via: submission insert and points increment both consume the evaluator's canonical score output
      pattern: aiScore
    - from: src/app/[client_id]/m/submit/actions.ts
      to: src/lib/ai-evaluator.ts
      via: mobile submission mirrors the same canonical score path
      pattern: aiScore
---

<objective>
Adopt the canonical score contract in the live submission paths that actually write ideas and award points.

Purpose: Fixing the evaluator alone is not enough if submit actions still treat alternate score values or fallback logic as equivalent. This plan makes both submit funnels persist and award from the same canonical score value for every new submission.
Output: Desktop and mobile submit actions that write canonical `ai_score`, reuse that same value for point updates, and stop relying on any separate trusted score input.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-score-integrity/05-VALIDATION.md
@.planning/phases/05-score-integrity/05-01-PLAN.md
@src/app/[client_id]/submit/actions.ts
@src/app/[client_id]/m/submit/actions.ts
@src/lib/ai-evaluator.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route both submit flows through one canonical score value</name>
  <files>src/app/[client_id]/submit/actions.ts, src/app/[client_id]/m/submit/actions.ts, src/lib/ai-evaluator.ts</files>
  <action>Update both submit actions so the inserted `ideas.ai_score`, any email score passed to notifications, and the points-to-add value all come from the same canonical evaluator field. Remove any assumptions that the model's raw `overall_score` can still be trusted independently.</action>
  <verify>npm run lint</verify>
  <done>New idea rows and point deltas are created from one score source in both submit entry points.</done>
</task>

<task type="auto">
  <name>Task 2: Harden fallback paths so score writes cannot drift</name>
  <files>src/app/[client_id]/submit/actions.ts, src/app/[client_id]/m/submit/actions.ts</files>
  <action>Audit the points increment fallback behavior and any direct `users.total_points` updates so they reuse the same already-persisted canonical score variable rather than recomputing or trusting alternate values. Where practical, centralize the fallback branch structure so future score changes do not fork desktop and mobile behavior again.</action>
  <verify>npm run build</verify>
  <done>Submission fallbacks still preserve the same score semantics when the primary points update path fails.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes
- [ ] `npm run build` passes
- [ ] One desktop and one mobile submission persist identical score semantics given the same evaluator payload
</verification>

<success_criteria>

- All tasks completed
- New submissions persist canonical scores in both submit paths
- Point awards follow the exact same canonical value
- The submit layer no longer has duplicate trusted score logic
</success_criteria>

<output>
After completion, create `.planning/phases/05-score-integrity/05-02-SUMMARY.md`
</output>
