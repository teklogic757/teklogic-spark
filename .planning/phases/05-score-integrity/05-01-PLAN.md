---
phase: 05-score-integrity
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/score.ts
  - src/lib/ai-evaluator.ts
autonomous: true
requirements:
  - SCOR-01
user_setup: []
must_haves:
  truths:
    - Weighted rubric math is implemented in one reusable server-side scoring utility instead of being implied only in prompt text
    - The persisted canonical score is derived from criterion weights and criterion scores even when the model returns a conflicting standalone overall score
    - Mismatch between rubric math and model-supplied overall score is explicitly ignored or recorded rather than silently becoming the saved score
  artifacts:
    - path: src/lib/score.ts
      provides: Shared rubric-weighted scoring helper plus mismatch detection contract
      min_lines: 45
    - path: src/lib/ai-evaluator.ts
      provides: Evaluator flow that returns the canonical rubric score as its authoritative score output
      min_lines: 120
  key_links:
    - from: src/lib/ai-evaluator.ts
      to: src/lib/score.ts
      via: evaluator normalizes model output through one canonical score calculator
      pattern: computeCanonicalWeightedScore
---

<objective>
Establish the score contract Phase 5 depends on by making rubric math executable code rather than a prompt-only expectation.

Purpose: The current evaluator computes weighted math but still trusts the model's `overall_score`. This plan creates one canonical scoring helper and makes the evaluator return that value as the source of truth for persisted scoring.
Output: A reusable score utility and an evaluator contract that clamps, rounds, and explains score mismatches in one place.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-score-integrity/05-VALIDATION.md
@src/lib/ai-evaluator.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a shared canonical weighted-score helper</name>
  <files>src/lib/score.ts, src/lib/ai-evaluator.ts</files>
  <action>Create a small server-safe utility that accepts weighted rubric definitions plus returned criterion scores, computes the weighted average, applies consistent rounding/clamping, and reports whether the model's `overall_score` materially differs from rubric math. Keep the helper framework-agnostic so submit flows and future tests can call it directly.</action>
  <verify>npm run lint</verify>
  <done>The codebase has one explicit implementation of the scoring rubric behind SCOR-01.</done>
</task>

<task type="auto">
  <name>Task 2: Make the evaluator return canonical score data and mismatch metadata</name>
  <files>src/lib/ai-evaluator.ts, src/lib/score.ts</files>
  <action>Refactor `evaluateIdea()` so it parses the model JSON, derives the canonical weighted score through the new helper, and uses that value as the authoritative score on the returned object. Preserve the model-provided score only as optional diagnostic metadata for logging or storage, and ensure conflicting values are never treated as persisted truth.</action>
  <verify>npm run build</verify>
  <done>Every caller of `evaluateIdea()` receives the rubric-derived score by default.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes
- [ ] `npm run build` passes
- [ ] A forced evaluator mismatch shows canonical score follows rubric math, not the model's standalone total
</verification>

<success_criteria>

- All tasks completed
- Canonical scoring is defined in code, not just in prompt instructions
- Conflicting model totals cannot silently overwrite rubric math
- Downstream plans can reuse one scoring contract
</success_criteria>

<output>
After completion, create `.planning/phases/05-score-integrity/05-01-SUMMARY.md`
</output>
