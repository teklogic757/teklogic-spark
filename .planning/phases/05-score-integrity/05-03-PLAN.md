---
phase: 05-score-integrity
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - supabase/migrations/20260304_reconcile_score_integrity.sql
  - src/lib/leaderboard.ts
  - src/app/[client_id]/dashboard/page.tsx
  - src/app/[client_id]/leaderboard/page.tsx
  - src/app/[client_id]/m/page.tsx
  - src/app/[client_id]/m/leaderboard/page.tsx
  - src/lib/contest-digest.ts
autonomous: true
requirements:
  - SCOR-02
user_setup: []
must_haves:
  truths:
    - Existing score-driven read models are reconciled so ranking and displayed point totals align with canonical `ideas.ai_score`
    - Dashboard, leaderboard, mobile score surfaces, and contest digest reads use one shared aggregation path instead of ad hoc `users.total_points` queries
    - Legacy score drift is reduced by a one-time reconciliation step rather than leaving pre-phase data to permanently skew ranking
  artifacts:
    - path: supabase/migrations/20260304_reconcile_score_integrity.sql
      provides: Backfill or reconciliation logic that aligns stored point totals and leaderboard source data with canonical idea scores
      min_lines: 30
    - path: src/lib/leaderboard.ts
      provides: Shared tenant-safe score aggregation helpers for leaderboard and point-summary reads
      min_lines: 60
    - path: src/app/[client_id]/dashboard/page.tsx
      provides: Dashboard stats and leaderboard reads that consume the shared canonical aggregate
      min_lines: 100
    - path: src/lib/contest-digest.ts
      provides: Digest payload built from the same canonical score aggregate used by UI reads
      min_lines: 120
  key_links:
    - from: src/lib/leaderboard.ts
      to: src/app/[client_id]/dashboard/page.tsx
      via: dashboard stats and leaderboard panels read canonical totals from one helper
      pattern: getOrganizationLeaderboard
    - from: src/lib/leaderboard.ts
      to: src/app/[client_id]/leaderboard/page.tsx
      via: full leaderboard page stops issuing ad hoc score queries
      pattern: getOrganizationLeaderboard
    - from: src/lib/leaderboard.ts
      to: src/lib/contest-digest.ts
      via: digest email payload uses the same point aggregation contract
      pattern: getOrganizationLeaderboard
---

<objective>
Reconcile the score read model so ranking, displayed totals, and digest content all reflect the canonical stored score contract.

Purpose: Phase 5 is incomplete if only new writes are fixed while historical totals and score-driven UIs still read from drift-prone paths. This plan backfills the obvious legacy mismatch and routes the major score displays through one shared aggregate.
Output: A reconciliation migration plus shared leaderboard helpers used by dashboard, leaderboard, mobile surfaces, and digest generation.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-score-integrity/05-VALIDATION.md
@.planning/phases/05-score-integrity/05-01-PLAN.md
@.planning/phases/05-score-integrity/05-02-PLAN.md
@supabase/migrations/20250204_create_user_leaderboard_view.sql
@src/app/[client_id]/dashboard/page.tsx
@src/app/[client_id]/leaderboard/page.tsx
@src/app/[client_id]/m/page.tsx
@src/app/[client_id]/m/leaderboard/page.tsx
@src/lib/contest-digest.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile legacy score totals and codify the aggregate contract</name>
  <files>supabase/migrations/20260304_reconcile_score_integrity.sql, src/lib/leaderboard.ts</files>
  <action>Add a migration that recomputes or safely realigns stale score-derived totals from persisted `ideas.ai_score` where data exists, and document any rows that cannot be perfectly repaired from stored analysis. In application code, introduce a small shared helper for tenant-scoped leaderboard and point-summary reads so score surfaces stop hand-rolling totals.</action>
  <verify>npm run lint</verify>
  <done>The repo has both a data-repair path and a shared canonical aggregation contract.</done>
</task>

<task type="auto">
  <name>Task 2: Move score-facing UI and digest reads onto the shared aggregate</name>
  <files>src/app/[client_id]/dashboard/page.tsx, src/app/[client_id]/leaderboard/page.tsx, src/app/[client_id]/m/page.tsx, src/app/[client_id]/m/leaderboard/page.tsx, src/lib/contest-digest.ts, src/lib/leaderboard.ts</files>
  <action>Replace direct `users.total_points` ranking queries in the dashboard, leaderboard pages, mobile landing page, mobile leaderboard, and contest digest with the shared helper created in Task 1. Keep the data tenant-scoped and ensure ordering, displayed totals, and digest ranking all come from the same canonical aggregate path.</action>
  <verify>npm run build</verify>
  <done>Major score-driven UI and digest reads agree on totals and ordering because they use one path.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes
- [ ] `npm run build` passes
- [ ] Manual spot-check shows one user's summed `ideas.ai_score` matches dashboard stats, leaderboard rank, and digest payload ordering after reconciliation
</verification>

<success_criteria>

- All tasks completed
- Score-based reads no longer depend on divergent ad hoc totals
- Existing drift is reduced instead of preserved indefinitely
- Ranking and point display share one canonical aggregation path
</success_criteria>

<output>
After completion, create `.planning/phases/05-score-integrity/05-03-SUMMARY.md`
</output>
