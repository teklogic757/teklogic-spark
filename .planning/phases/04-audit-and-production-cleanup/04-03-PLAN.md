---
phase: 04-audit-and-production-cleanup
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/app/login/page.tsx
  - src/app/login/login-form.tsx
  - src/app/auth/callback/route.ts
  - src/app/[client_id]/dashboard/page.tsx
  - src/app/[client_id]/submit/actions.ts
  - src/app/[client_id]/m/submit/actions.ts
  - src/app/admin/actions.ts
  - src/lib/server-log.ts
autonomous: true
requirements:
  - QUAL-03
user_setup: []
must_haves:
  truths:
    - Core auth, dashboard, submit, and admin flows stop narrating routine success paths in production logs
    - Legitimate warnings and errors still preserve enough detail to diagnose failures without dumping sensitive request/session context
    - Remaining diagnostics are centralized enough that future logging changes do not require another broad console-cleanup pass
  artifacts:
    - path: src/app/[client_id]/dashboard/page.tsx
      provides: Cleaned dashboard request flow without verbose cookie/session debug output
      min_lines: 60
    - path: src/app/[client_id]/submit/actions.ts
      provides: Submit flow that keeps actionable warnings/errors while removing routine progress narration
      min_lines: 80
    - path: src/lib/server-log.ts
      provides: Shared helper adopted by production-facing request paths for safe diagnostics
      min_lines: 40
  key_links:
    - from: src/app/[client_id]/submit/actions.ts
      to: src/lib/server-log.ts
      via: remaining warnings/errors are routed through the shared helper where appropriate
      pattern: (warn|error)
    - from: src/app/login/page.tsx
      to: src/lib/server-log.ts
      via: login flow stops logging routine session state directly
      pattern: debug
---

<objective>
Remove noisy debug logging from the core production request paths while keeping useful failure diagnostics.

Purpose: Satisfy QUAL-03 by cleaning up the logs users and operators hit most often. This plan targets the login, dashboard, submit, and admin paths that currently emit routine progress logs or over-detailed request context.
Output: Production-facing flows keep actionable warnings/errors, but normal request handling no longer fills logs with control-flow narration or unnecessary session details.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-audit-and-production-cleanup/04-RESEARCH.md
@.planning/phases/04-audit-and-production-cleanup/04-VALIDATION.md
@src/app/login/page.tsx
@src/app/login/login-form.tsx
@src/app/auth/callback/route.ts
@src/app/[client_id]/dashboard/page.tsx
@src/app/[client_id]/submit/actions.ts
@src/app/[client_id]/m/submit/actions.ts
@src/app/admin/actions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove or gate noisy logs in auth and dashboard flows</name>
  <files>src/app/login/page.tsx, src/app/login/login-form.tsx, src/app/auth/callback/route.ts, src/app/[client_id]/dashboard/page.tsx, src/lib/server-log.ts</files>
  <action>Delete routine success-path console logging from login and dashboard rendering, especially cookie/session narration and redirect chatter. Where limited debug diagnostics are still useful, route them through the shared helper so they are development-only. Preserve explicit error logs for true auth or org-load failures with sanitized payloads.</action>
  <verify>npm run lint</verify>
  <done>Auth and dashboard paths stop producing noisy operational logs in normal usage.</done>
</task>

<task type="auto">
  <name>Task 2: Trim submit/admin flow log noise while preserving useful warning and error signals</name>
  <files>src/app/[client_id]/submit/actions.ts, src/app/[client_id]/m/submit/actions.ts, src/app/admin/actions.ts, src/lib/server-log.ts</files>
  <action>Remove progress and success narration from the submit and admin action flows, especially repeated validation/insert/update messages. Keep warnings and errors for genuinely actionable failures, but normalize the logged payloads so they identify the subsystem without dumping broad request context or sensitive user/session details.</action>
  <verify>npm run build</verify>
  <done>The highest-traffic mutation paths retain diagnosable failure logs without production noise.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes
- [ ] `npm run build` passes
- [ ] Manual smoke check confirms routine login/dashboard/submit requests no longer emit verbose success-path logs while one forced failure still logs a useful diagnostic
</verification>

<success_criteria>

- All tasks completed
- Routine production request paths are substantially quieter
- Useful warnings/errors remain available for real failures
- Logging behavior is centralized enough to avoid future copy-paste console drift
</success_criteria>

<output>
After completion, create `.planning/phases/04-audit-and-production-cleanup/04-03-SUMMARY.md`
</output>
