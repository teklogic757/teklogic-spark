---
phase: 04-audit-and-production-cleanup
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/app/admin/actions.ts
  - src/app/admin/workshops/actions.ts
  - src/lib/audit-log.ts
autonomous: true
requirements:
  - QUAL-04
user_setup: []
must_haves:
  truths:
    - Every sensitive admin mutation in the existing admin surfaces writes an auditable record after the underlying mutation succeeds
    - Audit records capture who acted, what action ran, what entity changed, and when it happened
    - The admin layer does not silently drop traceability when an audit write fails
  artifacts:
    - path: src/app/admin/actions.ts
      provides: Instrumented super-admin mutations for organization, user, invitation, and training-video changes
      min_lines: 90
    - path: src/app/admin/workshops/actions.ts
      provides: Instrumented workshop-code mutations so the auxiliary admin surface is also covered
      min_lines: 30
    - path: src/lib/audit-log.ts
      provides: Finalized helper contract used by both admin mutation files
      min_lines: 60
  key_links:
    - from: src/app/admin/actions.ts
      to: src/lib/audit-log.ts
      via: privileged mutations emit normalized audit events through the shared helper
      pattern: writeAdminAuditEvent
    - from: src/app/admin/workshops/actions.ts
      to: src/lib/audit-log.ts
      via: workshop-code changes reuse the same audit pipeline
      pattern: writeAdminAuditEvent
---

<objective>
Instrument the current privileged admin mutations so successful changes become traceable.

Purpose: Satisfy QUAL-04 in the real server-action paths admins use today, not just at the schema layer. This plan wires the shared audit helper into the live mutation surfaces that can materially change tenant, user, and workshop state.
Output: Existing admin mutation actions emit durable audit events with actor, action, target, and timestamp details.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-audit-and-production-cleanup/04-RESEARCH.md
@.planning/phases/04-audit-and-production-cleanup/04-VALIDATION.md
@src/app/admin/actions.ts
@src/app/admin/workshops/actions.ts
@src/lib/audit-log.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instrument core super-admin mutations in the main admin action file</name>
  <files>src/app/admin/actions.ts, src/lib/audit-log.ts</files>
  <action>Add audit writes for the sensitive mutations already exposed in `src/app/admin/actions.ts`, including organization create/update, user create/update, invitation creation, and training-video create/delete. Each event should identify the actor, action name, target entity, target ID when available, and only minimal safe metadata.</action>
  <verify>npm run lint</verify>
  <done>The main admin server actions now leave a durable trace record for each sensitive mutation.</done>
</task>

<task type="auto">
  <name>Task 2: Extend audit coverage to workshop admin mutations and failure handling</name>
  <files>src/app/admin/workshops/actions.ts, src/lib/audit-log.ts</files>
  <action>Instrument workshop access-code creation and status toggles with the same audit helper. Ensure the action response path does not report full success if the underlying mutation cannot be traced, so operators are not misled about compliance with QUAL-04.</action>
  <verify>npm run build</verify>
  <done>The secondary admin surface is covered and the audit contract is consistently enforced.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes
- [ ] `npm run build` passes
- [ ] Manual smoke check confirms at least one admin mutation produces a matching audit row in the database
</verification>

<success_criteria>

- All tasks completed
- Sensitive admin mutation paths are instrumented, not just the schema
- Audit events include the minimum required trace fields for QUAL-04
- Admin actions no longer silently succeed without traceability
</success_criteria>

<output>
After completion, create `.planning/phases/04-audit-and-production-cleanup/04-02-SUMMARY.md`
</output>
