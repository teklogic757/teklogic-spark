---
phase: 04-audit-and-production-cleanup
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260303_add_admin_audit_event_support.sql
  - src/lib/types/database.types.ts
  - src/lib/audit-log.ts
  - src/lib/server-log.ts
autonomous: true
requirements:
  - QUAL-03
  - QUAL-04
user_setup: []
must_haves:
  truths:
    - The app has one shared persistence model for sensitive admin audit events instead of ad hoc inline logging
    - Audit writes and production-safe logging both flow through reusable server-side helpers rather than duplicated console calls
    - Downstream plans can instrument admin actions and trim noisy logs without redefining the core observability contract
  artifacts:
    - path: supabase/migrations/20260303_add_admin_audit_event_support.sql
      provides: Schema support for durable admin audit events
      min_lines: 35
    - path: src/lib/audit-log.ts
      provides: Shared server-only helper for writing normalized admin audit records
      min_lines: 50
    - path: src/lib/server-log.ts
      provides: Shared debug-gated and sanitized logging helpers for production-facing flows
      min_lines: 40
  key_links:
    - from: src/lib/audit-log.ts
      to: src/app/admin/actions.ts
      via: admin mutations call one helper after successful writes
      pattern: writeAdminAuditEvent
    - from: src/lib/server-log.ts
      to: src/app/[client_id]/dashboard/page.tsx
      via: noisy debug statements are removed or routed through one gated helper
      pattern: debug
---

<objective>
Create the shared audit and logging primitives Phase 4 depends on before touching the individual request flows.

Purpose: Prevent the admin instrumentation and production-log cleanup work from being implemented as one-off console changes. This plan establishes the durable audit schema plus the reusable helpers that downstream plans should consume.
Output: A migration, updated types, and shared server-only utilities for audit events and production-safe logging.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-audit-and-production-cleanup/04-RESEARCH.md
@.planning/phases/04-audit-and-production-cleanup/04-VALIDATION.md
@src/lib/types/database.types.ts
@src/app/admin/actions.ts
@src/app/[client_id]/dashboard/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add audit-event schema support and generated-style types</name>
  <files>supabase/migrations/20260303_add_admin_audit_event_support.sql, src/lib/types/database.types.ts</files>
  <action>Create one migration that introduces a dedicated admin audit-events table with actor, action, target, optional minimal metadata, and timestamp fields. Update the local database types so server code can insert/read audit records without `any` fallbacks.</action>
  <verify>npm run lint</verify>
  <done>The database can durably store the trace records required by QUAL-04.</done>
</task>

<task type="auto">
  <name>Task 2: Add shared audit-write and safe logging helpers</name>
  <files>src/lib/audit-log.ts, src/lib/server-log.ts</files>
  <action>Implement a server-only audit helper that accepts normalized event details and persists them through the appropriate Supabase client. Add a small logging helper that gates debug output outside production and normalizes unknown errors into safe, actionable log payloads without dumping cookies or large objects.</action>
  <verify>npm run build</verify>
  <done>Downstream plans can rely on one observability contract instead of duplicating inline logging behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes
- [ ] `npm run build` passes
- [ ] Manual schema review confirms the audit table captures actor, action, target, and timestamp
</verification>

<success_criteria>

- All tasks completed
- The repo has concrete persistence support for admin audit events
- Shared helpers exist for both audit writes and production-safe diagnostics
- Downstream flow changes can be implemented against a stable observability foundation
</success_criteria>

<output>
After completion, create `.planning/phases/04-audit-and-production-cleanup/04-01-SUMMARY.md`
</output>
