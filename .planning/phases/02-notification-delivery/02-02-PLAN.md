---
phase: 02-notification-delivery
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/app/admin/actions.ts
  - src/app/[client_id]/submit/actions.ts
  - src/app/[client_id]/m/submit/actions.ts
autonomous: true
requirements:
  - NOTF-01
  - NOTF-02
  - NOTF-03
user_setup: []
must_haves:
  truths:
    - Admin user creation attempts a welcome email with organization-specific login guidance and logs the normalized delivery outcome without blocking the redirect
    - Desktop and mobile idea submissions both attempt evaluation and admin notifications after the idea is saved
    - Notification failures never invalidate a successful user creation or idea submission, but they are no longer silently misinterpreted
  artifacts:
    - path: src/app/admin/actions.ts
      provides: User creation path that consumes the shared notification contract for welcome emails
      min_lines: 60
    - path: src/app/[client_id]/submit/actions.ts
      provides: Desktop submit path with structured evaluation and admin notification handling
      min_lines: 120
    - path: src/app/[client_id]/m/submit/actions.ts
      provides: Mobile submit path aligned to the same notification contract, including admin notifications
      min_lines: 90
  key_links:
    - from: src/app/admin/actions.ts
      to: src/lib/email.ts
      via: normalized welcome-email result handling
      pattern: sendWelcomeEmail
    - from: src/app/[client_id]/submit/actions.ts
      to: src/lib/email.ts
      via: normalized evaluation and admin notification result handling
      pattern: send(EvaluationEmail|AdminNotification)
    - from: src/app/[client_id]/m/submit/actions.ts
      to: src/lib/email.ts
      via: notification parity with the desktop submit flow
      pattern: send(EvaluationEmail|AdminNotification)
---

<objective>
Integrate the shared notification contract into the admin and submit actions so the real product flows deliver the right emails and fail safely.

Purpose: Complete the user-facing behavior of Phase 2 after the transport layer is stable.
Output: Updated admin, desktop submit, and mobile submit actions that route notifications correctly, log explicit outcomes, and preserve successful primary mutations even when delivery is skipped or fails.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-notification-delivery/02-RESEARCH.md
@.planning/phases/02-notification-delivery/02-VALIDATION.md
@src/app/admin/actions.ts
@src/app/[client_id]/submit/actions.ts
@src/app/[client_id]/m/submit/actions.ts
@src/lib/email.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden the admin user-creation notification flow</name>
  <files>src/app/admin/actions.ts</files>
  <action>Update `createUser()` so it consumes the normalized welcome-email result contract and logs delivery outcomes accurately. Keep the mutation non-blocking, but make the welcome-email guidance organization-specific and avoid treating skipped, delivered, and failed results as the same kind of console event.</action>
  <verify>npm run lint</verify>
  <done>Creating a user still redirects normally, while the welcome-email path reports meaningful outcome states.</done>
</task>

<task type="auto">
  <name>Task 2: Align the desktop submit flow with the notification contract</name>
  <files>src/app/[client_id]/submit/actions.ts</files>
  <action>Refactor the desktop submit action to use the shared delivery contract for both evaluation and admin notifications. Preserve the current non-blocking pattern, but remove placeholder-style misrouting where possible, improve recipient resolution for admin notifications, and keep attachment metadata attached to the admin-notification path.</action>
  <verify>npm run build</verify>
  <done>The desktop submission path saves ideas first, then attempts the correct notifications with explicit success, skip, or failure handling.</done>
</task>

<task type="auto">
  <name>Task 3: Add mobile admin-notification parity</name>
  <files>src/app/[client_id]/m/submit/actions.ts</files>
  <action>Bring the mobile submit action up to the same notification contract as the desktop flow by handling provider result states explicitly and adding the missing admin-notification attempt after a successful idea save. Keep the existing mobile redirect behavior intact, and do not allow notification issues to break the successful submission path.</action>
  <verify>npm run build</verify>
  <done>Mobile submissions trigger both evaluation and admin notifications under the same reliability rules as the desktop path.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes
- [ ] `npm run build` passes
- [ ] Manual smoke checks cover admin user creation plus one desktop and one mobile submission
</verification>

<success_criteria>

- All tasks completed
- Welcome, evaluation, and admin notification flows all consume the same delivery contract
- Mobile submit now triggers the admin notification path
- Notification failures are visible and non-blocking across all core Phase 2 actions
</success_criteria>

<output>
After completion, create `.planning/phases/02-notification-delivery/02-02-SUMMARY.md`
</output>
