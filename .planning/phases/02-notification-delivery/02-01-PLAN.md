---
phase: 02-notification-delivery
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/email.ts
autonomous: true
requirements:
  - NOTF-01
  - NOTF-02
  - NOTF-03
user_setup: []
must_haves:
  truths:
    - The shared notification layer returns an explicit delivery state that distinguishes delivered, skipped, and failed outcomes
    - Provider-specific differences stay inside the email library rather than leaking into server actions
    - Admin notification payloads preserve attachment handling across supported provider paths or report an explicit downgrade instead of silently dropping data
  artifacts:
    - path: src/lib/email.ts
      provides: A normalized notification contract plus shared provider handling for welcome, evaluation, and admin emails
      min_lines: 80
      exports:
        - sendEmail
        - sendWelcomeEmail
        - sendEvaluationEmail
        - sendAdminNotification
  key_links:
    - from: src/lib/email.ts
      to: src/lib/email.ts
      via: provider-specific helpers normalizing to one contract
      pattern: sendVia(SMTP|Resend)
---

<objective>
Refactor the notification library so all email flows share one explicit delivery contract and one reliable provider abstraction.

Purpose: De-risk Phase 2 by fixing the foundation first. Downstream actions should be able to attempt notifications without guessing whether a provider error, missing credentials, or attachment limitation occurred.
Output: An upgraded `src/lib/email.ts` that normalizes delivery status, keeps test overrides centralized, and preserves attachment behavior across providers.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-notification-delivery/02-RESEARCH.md
@.planning/phases/02-notification-delivery/02-VALIDATION.md
@src/lib/email.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce a normalized notification result contract</name>
  <files>src/lib/email.ts</files>
  <action>Replace the current boolean-style `EmailResult` semantics with a richer result shape that clearly represents delivered, skipped, and failed outcomes. Keep the contract provider-agnostic, and make sure missing credentials, unknown providers, or payload issues return explicit status metadata without throwing by default.</action>
  <verify>npm run lint</verify>
  <done>Server actions can inspect a consistent notification result without inferring meaning from a bare `success` flag.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize provider behavior and attachment support</name>
  <files>src/lib/email.ts</files>
  <action>Update the SMTP and Resend code paths so they both honor the same recipient override, subject rewrite, and attachment expectations used by admin notifications. If a provider cannot fully support the payload, surface that as an explicit result state or warning inside the contract rather than silently discarding attachment data.</action>
  <verify>npm run build</verify>
  <done>The email library can send or explicitly downgrade notifications in a predictable way across configured provider paths.</done>
</task>

<task type="auto">
  <name>Task 3: Tighten helper-level notification semantics</name>
  <files>src/lib/email.ts</files>
  <action>Adjust the helper functions for welcome, evaluation, guest welcome, and admin notifications so they all flow through the normalized contract and encode Phase 2 expectations cleanly. That includes using the right organization-specific login guidance for welcome emails and keeping admin-notification metadata and attachment payloads intact.</action>
  <verify>npm run lint</verify>
  <done>The notification helpers are ready for action-level integration without duplicating transport logic elsewhere.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes
- [ ] `npm run build` passes
- [ ] Manual spot check confirms the email layer can distinguish delivered vs skipped vs failed outcomes in logs or returned results
</verification>

<success_criteria>

- All tasks completed
- `src/lib/email.ts` exposes one reusable delivery contract for all notification flows
- Provider-specific attachment and credential edge cases are surfaced explicitly
- Welcome and admin helper functions are ready for reliable integration in Phase 2 call sites
</success_criteria>

<output>
After completion, create `.planning/phases/02-notification-delivery/02-01-SUMMARY.md`
</output>
