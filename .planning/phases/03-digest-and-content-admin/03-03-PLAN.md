---
phase: 03-digest-and-content-admin
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/app/admin/actions.ts
  - src/app/admin/layout.tsx
  - src/app/admin/training/page.tsx
  - src/app/[client_id]/dashboard/page.tsx
  - src/components/features/dashboard/TrainingResources.tsx
  - src/data/training-videos.ts
autonomous: true
requirements:
  - ADMN-01
  - ADMN-02
user_setup: []
must_haves:
  truths:
    - Super admins can add a YouTube video from the admin UI without editing source files
    - Super admins can remove an existing training item from the same admin flow
    - Tenant dashboards render the persisted learning library so admin changes show up through normal app usage
  artifacts:
    - path: src/app/admin/training/page.tsx
      provides: Admin UI for listing, adding, and deleting training videos
      min_lines: 80
    - path: src/app/admin/actions.ts
      provides: Super-admin-protected server actions for training-video CRUD
      min_lines: 60
    - path: src/components/features/dashboard/TrainingResources.tsx
      provides: Client component that renders server-provided training-video data instead of owning static content
      min_lines: 40
  key_links:
    - from: src/app/admin/training/page.tsx
      to: src/app/admin/actions.ts
      via: form actions for create and delete
      pattern: (create|delete)TrainingVideo
    - from: src/app/[client_id]/dashboard/page.tsx
      to: src/components/features/dashboard/TrainingResources.tsx
      via: server fetch passes managed video records into the client renderer
      pattern: TrainingResources
---

<objective>
Replace the static training-video library with an admin-managed flow and wire the dashboard to it.

Purpose: Close the learning-library admin gap without introducing a large CMS. Super admins should be able to add and remove videos inside the existing admin area, and tenants should see the updated library immediately.
Output: A dedicated admin training page plus dashboard integration that consumes persisted video records instead of source-controlled constants.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-digest-and-content-admin/03-RESEARCH.md
@.planning/phases/03-digest-and-content-admin/03-VALIDATION.md
@src/app/admin/actions.ts
@src/app/admin/layout.tsx
@src/app/[client_id]/dashboard/page.tsx
@src/components/features/dashboard/TrainingResources.tsx
@src/data/training-videos.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add super-admin CRUD actions and navigation for training videos</name>
  <files>src/app/admin/actions.ts, src/app/admin/layout.tsx, src/app/admin/training/page.tsx</files>
  <action>Implement super-admin-protected server actions to list, create, and delete training videos using the new shared validation/parsing helpers. Add a dedicated `/admin/training` page to expose those actions, and link it from the admin shell so the feature is discoverable.</action>
  <verify>npm run lint</verify>
  <done>Super admins can manage the learning library entirely from the existing admin area.</done>
</task>

<task type="auto">
  <name>Task 2: Replace static dashboard training data with persisted content</name>
  <files>src/app/[client_id]/dashboard/page.tsx, src/components/features/dashboard/TrainingResources.tsx, src/data/training-videos.ts</files>
  <action>Refactor the tenant dashboard to fetch training videos on the server and pass them into `TrainingResources` as props. Reduce `src/data/training-videos.ts` to a fallback or compatibility layer only if needed, and make sure deletes/adds from the admin path are reflected without code changes.</action>
  <verify>npm run build</verify>
  <done>The dashboard renders the admin-managed training library instead of a hard-coded list.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes
- [ ] `npm run build` passes
- [ ] Manual smoke check confirms one add and one delete are reflected on a tenant dashboard
</verification>

<success_criteria>

- All tasks completed
- The admin area exposes working add/remove training-video management
- Dashboard training resources are driven by persisted content
- Source edits are no longer required for routine library maintenance
</success_criteria>

<output>
After completion, create `.planning/phases/03-digest-and-content-admin/03-03-SUMMARY.md`
</output>
