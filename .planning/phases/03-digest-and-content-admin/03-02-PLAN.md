---
phase: 03-digest-and-content-admin
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/lib/email.ts
  - src/lib/contest-digest.ts
  - src/app/api/cron/contest-digest/route.ts
autonomous: true
requirements:
  - NOTF-04
user_setup: []
must_haves:
  truths:
    - There is a callable server-side path that can be scheduled externally and it exits cleanly when no contest is active
    - Contest digest delivery uses explicit active-window and once-per-week guards before attempting sends
    - Digest content includes both leaderboard standings and recent top ideas so the email reflects actual contest activity
  artifacts:
    - path: src/app/api/cron/contest-digest/route.ts
      provides: Cron-ready route that discovers eligible organizations and triggers digest sends
      min_lines: 60
    - path: src/lib/contest-digest.ts
      provides: Shared query and eligibility logic for active contests plus digest payload assembly
      min_lines: 80
    - path: src/lib/email.ts
      provides: Weekly contest digest email generation and delivery helper
      min_lines: 40
  key_links:
    - from: src/app/api/cron/contest-digest/route.ts
      to: src/lib/contest-digest.ts
      via: route delegates contest eligibility and payload building
      pattern: sendWeeklyContestDigests
    - from: src/lib/contest-digest.ts
      to: src/lib/email.ts
      via: assembled digest payload rendered and sent through the shared email layer
      pattern: sendContestDigestEmail
---

<objective>
Implement the weekly contest digest as a real server-side delivery path.

Purpose: Satisfy NOTF-04 with a route the platform can invoke on a schedule instead of a manual placeholder or comment-only contract.
Output: A cron-safe digest workflow that filters for active contests, builds digest summaries from current data, sends them through the email layer, and records the last run timestamp.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-digest-and-content-admin/03-RESEARCH.md
@.planning/phases/03-digest-and-content-admin/03-VALIDATION.md
@src/lib/email.ts
@src/app/admin/actions.ts
@src/app/[client_id]/dashboard/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build contest eligibility and digest payload helpers</name>
  <files>src/lib/contest-digest.ts, src/lib/email.ts</files>
  <action>Create shared server-only helpers that find organizations with active contests, enforce the seven-day cadence using `last_contest_digest_sent_at`, query top users and top ideas, and render/send a digest email through the existing notification abstraction.</action>
  <verify>npm run lint</verify>
  <done>The digest behavior is encoded in reusable helpers instead of inline route logic.</done>
</task>

<task type="auto">
  <name>Task 2: Expose a cron-ready digest route</name>
  <files>src/app/api/cron/contest-digest/route.ts</files>
  <action>Add a route handler that verifies a shared secret, invokes the shared digest helper, returns a structured summary of processed vs skipped organizations, and never sends digests for inactive or expired contests.</action>
  <verify>npm run build</verify>
  <done>The app has a defined scheduled-style path that can be called by a cron provider.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes
- [ ] `npm run build` passes
- [ ] Manual smoke check shows one eligible contest sends once, then skips on the next immediate retry
</verification>

<success_criteria>

- All tasks completed
- The app exposes one concrete contest-digest route
- Digests only run for active contests and honor weekly cadence
- Digest content is derived from live leaderboard and idea data
</success_criteria>

<output>
After completion, create `.planning/phases/03-digest-and-content-admin/03-02-SUMMARY.md`
</output>
