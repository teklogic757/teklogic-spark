---
phase: 03-digest-and-content-admin
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260303_add_digest_and_training_video_support.sql
  - src/lib/types/database.types.ts
  - src/lib/validators.ts
  - src/lib/training-videos.ts
autonomous: true
requirements:
  - NOTF-04
  - ADMN-01
  - ADMN-02
user_setup: []
must_haves:
  truths:
    - The app persists the weekly digest cadence so the same organization cannot be re-sent a digest repeatedly inside one seven-day window
    - Training videos are stored in the database as managed content instead of being limited to source-controlled static data
    - YouTube URLs are normalized and validated in one shared server-safe helper before admin actions rely on them
  artifacts:
    - path: supabase/migrations/20260303_add_digest_and_training_video_support.sql
      provides: Schema support for digest cadence and managed training-video records
      min_lines: 40
    - path: src/lib/types/database.types.ts
      provides: Updated generated-style app types for the new digest and training-video fields
      min_lines: 20
    - path: src/lib/training-videos.ts
      provides: Reusable YouTube parsing and training-video normalization utilities for admin and dashboard flows
      min_lines: 40
  key_links:
    - from: src/lib/training-videos.ts
      to: src/app/admin/actions.ts
      via: shared normalization before insert/delete flows
      pattern: parseYouTube
    - from: src/lib/types/database.types.ts
      to: src/app/[client_id]/dashboard/page.tsx
      via: typed reads for training videos and digest cadence fields
      pattern: training_videos
---

<objective>
Create the persistence and shared domain primitives Phase 3 needs before feature work starts.

Purpose: Prevent the digest and admin-content work from being built on ad hoc state. This plan adds the schema, typing, and parsing contracts that both downstream plans depend on.
Output: A migration plus shared TypeScript helpers that make weekly digest scheduling and training-video CRUD implementable without duplicating logic.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-digest-and-content-admin/03-RESEARCH.md
@.planning/phases/03-digest-and-content-admin/03-VALIDATION.md
@src/lib/types/database.types.ts
@src/lib/validators.ts
@src/data/training-videos.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add schema support for digest cadence and training videos</name>
  <files>supabase/migrations/20260303_add_digest_and_training_video_support.sql, src/lib/types/database.types.ts</files>
  <action>Create one migration that adds `last_contest_digest_sent_at` to `organizations` and introduces a `training_videos` table for the shared learning library. Update the local database types so app code can read and write the new fields without falling back to `any`.</action>
  <verify>npm run lint</verify>
  <done>The database can now store both weekly digest state and admin-managed training items.</done>
</task>

<task type="auto">
  <name>Task 2: Centralize YouTube parsing and training-video validation</name>
  <files>src/lib/training-videos.ts, src/lib/validators.ts</files>
  <action>Move the current ad hoc video-ID extraction into a shared utility that handles common YouTube URL formats, derives thumbnail URLs, and returns a normalized shape the admin flow can trust. Add validation rules for add/remove operations so malformed input is rejected before any database write.</action>
  <verify>npm run build</verify>
  <done>Downstream admin and dashboard code can rely on one source of truth for YouTube normalization.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes
- [ ] `npm run build` passes
- [ ] Manual schema review confirms the cadence field and training-video table match the research decisions
</verification>

<success_criteria>

- All tasks completed
- The repo has concrete schema support for weekly digest cadence
- Training videos have a real persisted model
- Shared parsing and validation primitives exist for downstream feature plans
</success_criteria>

<output>
After completion, create `.planning/phases/03-digest-and-content-admin/03-01-SUMMARY.md`
</output>
