---
phase: 08-durable-abuse-controls
plan: "03"
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - src/lib/rate-limiter.ts
  - src/lib/submit-flow.ts
  - src/app/join/actions.ts
autonomous: true
requirements:
  - RATE-02
user_setup: []
must_haves:
  truths:
    - Workshop guest submissions no longer bypass throttling and instead use explicit durable limits tied to the guest flow's real identifiers
    - Attachment-adjacent work is protected before file upload and other higher-cost steps so guests cannot bypass protection by sending files repeatedly
    - Workshop code entry and guest submission continue to return clear user-facing failures when throttled instead of silently weakening the protection boundary
  artifacts:
    - path: src/lib/rate-limiter.ts
      provides: Separate durable presets or helper scopes for workshop join, guest submit, and attachment-heavy submit paths
      min_lines: 150
    - path: src/lib/submit-flow.ts
      provides: Guest/workshop and attachment-specific rate-limit checkpoints that run before AI evaluation and uploads
      min_lines: 240
    - path: src/app/join/actions.ts
      provides: Workshop join path using the same durable limiter before validating access codes
      min_lines: 120
  key_links:
    - from: src/app/join/actions.ts
      to: src/lib/rate-limiter.ts
      via: workshop code attempts consume a durable budget before the app looks up the code and sets cookies
      pattern: rateLimitAction
    - from: src/lib/submit-flow.ts
      to: src/lib/rate-limiter.ts
      via: guest submissions and attachment-adjacent work use explicit durable throttles instead of relying only on the authenticated submit limiter
      pattern: rateLimitAction
---

<objective>
Close the remaining abuse gaps by making workshop and guest submission paths explicitly durable and cost-aware.

Purpose: The authenticated submit path already had a limiter, but workshop guests currently bypass that branch entirely, and attachment work can still become an expensive bypass if limits are checked too late. This plan adds explicit durable throttles for workshop join attempts, guest submissions, and attachment-adjacent work so the public-facing workshop flow stays protected in real deployments.
Output: Durable throttling now covers the workshop entry point and the guest submission flow, including the costly attachment path.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-durable-abuse-controls/08-VALIDATION.md
@.planning/phases/08-durable-abuse-controls/08-01-PLAN.md
@.planning/phases/08-durable-abuse-controls/08-02-PLAN.md
@src/lib/rate-limiter.ts
@src/lib/submit-flow.ts
@src/app/join/actions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add durable guest and attachment-adjacent throttles to the submit flow</name>
  <files>src/lib/rate-limiter.ts, src/lib/submit-flow.ts</files>
  <action>Introduce explicit limiter scopes for workshop guests and any attachment-heavy submission path, then apply them before AI evaluation, storage writes, and file uploads begin. Derive the guest identifier from stable request data available to the server action (for example normalized IP plus workshop context) so the protection remains meaningful without a logged-in user id.</action>
  <verify>npm run lint</verify>
  <done>Workshop guests and attachment-heavy submits are explicitly throttled before the expensive work starts.</done>
</task>

<task type="auto">
  <name>Task 2: Protect the workshop join path without breaking the workshop redirect UX</name>
  <files>src/app/join/actions.ts, src/lib/rate-limiter.ts</files>
  <action>Add durable throttling to `joinWorkshop()` before access-code validation so repeated invalid attempts cannot brute-force workshop codes. Keep the current valid-code behavior intact: anonymous auth remains best-effort, the workshop cookie is still set, and successful joins still redirect into the organization submit flow after the new guard passes.</action>
  <verify>npm run build</verify>
  <done>The workshop entry path is throttled durably and valid joins still work as they do now.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes
- [ ] `npm run build` passes
- [ ] Workshop guest submissions with and without attachments hit the new limits before AI evaluation or file upload work begins
</verification>

<success_criteria>

- All tasks completed
- Guest workshop submits have durable throttling
- Attachment-adjacent submit work is protected before high-cost operations begin
- Workshop join attempts are throttled without regressing valid join redirects
</success_criteria>

<output>
After completion, create `.planning/phases/08-durable-abuse-controls/08-03-SUMMARY.md`
</output>
