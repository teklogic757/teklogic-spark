---
phase: 08-durable-abuse-controls
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/lib/rate-limiter.ts
  - src/lib/submit-flow.ts
  - src/app/login/actions.ts
  - src/app/admin/actions.ts
autonomous: true
requirements:
  - RATE-01
  - RATE-02
user_setup: []
must_haves:
  truths:
    - Existing authenticated and pre-auth entry points switch to the durable limiter contract instead of relying on per-process counters
    - Every call site passes an explicit, stable identifier such as user id or normalized client IP so throttling remains meaningful after the backend migration
    - Expensive work such as organization lookup, AI evaluation preparation, and admin mutations still fail fast when the limiter blocks the request
  artifacts:
    - path: src/lib/rate-limiter.ts
      provides: Awaitable limiter API and any helper utilities needed by server-action call sites
      min_lines: 140
    - path: src/lib/submit-flow.ts
      provides: Authenticated submission throttling wired to the durable backend before persistence and AI work
      min_lines: 220
    - path: src/app/login/actions.ts
      provides: Login organization lookup using the durable limiter with explicit IP normalization
      min_lines: 90
    - path: src/app/admin/actions.ts
      provides: Admin invitation and other guarded mutations using the same durable limiter contract
      min_lines: 650
  key_links:
    - from: src/app/login/actions.ts
      to: src/lib/rate-limiter.ts
      via: organization lookup throttling moves to the durable backend without changing the user-facing error shape
      pattern: rateLimitAction
    - from: src/app/admin/actions.ts
      to: src/lib/rate-limiter.ts
      via: admin invitation throttling uses the shared durable limiter contract
      pattern: rateLimitAction
    - from: src/lib/submit-flow.ts
      to: src/lib/rate-limiter.ts
      via: authenticated idea submissions are blocked before expensive downstream work when the durable budget is exhausted
      pattern: rateLimitAction
---

<objective>
Cut over the existing rate-limited server actions to the durable backend without changing their route-level UX.

Purpose: After the backend exists, the next risk is partial adoption where some actions still depend on in-memory behavior. This plan updates the current limiter call sites so authenticated submissions, login org lookup, and admin invitation flows all enforce limits through the durable store and fail early before doing unnecessary work.
Output: Core server actions all await one shared durable limiter contract with explicit identifiers.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-durable-abuse-controls/08-VALIDATION.md
@.planning/phases/08-durable-abuse-controls/08-01-PLAN.md
@src/lib/rate-limiter.ts
@src/lib/submit-flow.ts
@src/app/login/actions.ts
@src/app/admin/actions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update shared and existing action call sites to await durable limiter checks</name>
  <files>src/lib/rate-limiter.ts, src/app/login/actions.ts, src/app/admin/actions.ts, src/lib/submit-flow.ts</files>
  <action>Adjust the limiter API and its current callers so server actions explicitly await the durable consume call instead of reading local memory. Normalize identifier derivation where needed, especially IP-based flows, so repeated requests hit the same durable bucket even when proxy headers or formatting vary.</action>
  <verify>npm run lint</verify>
  <done>The existing throttled actions now use the durable backend consistently.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve fast-fail boundaries before expensive work</name>
  <files>src/lib/submit-flow.ts, src/app/login/actions.ts, src/app/admin/actions.ts</files>
  <action>Keep the limit checks positioned before costly downstream work: before login organization lookups, before admin invitation creation, and before authenticated submission evaluation or persistence. Preserve current user-facing error messages unless a clearer durable-limit message is needed, but do not let this refactor widen the amount of work done before a blocked request stops.</action>
  <verify>npm run build</verify>
  <done>Durable throttling protects the current hot paths without adding unnecessary work or changing route behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes
- [ ] `npm run build` passes
- [ ] Repeated login lookup, admin invite, and authenticated submit requests stay throttled after a server restart or across two app instances
</verification>

<success_criteria>

- All tasks completed
- Existing rate-limited call sites use the durable backend
- Stable identifiers are passed into every durable limiter check
- Hot paths still fail fast before expensive downstream work
</success_criteria>

<output>
After completion, create `.planning/phases/08-durable-abuse-controls/08-02-SUMMARY.md`
</output>
