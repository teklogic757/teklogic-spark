---
phase: 10-repository-and-secret-hygiene
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/env-validator.ts
  - src/lib/email.ts
  - src/app/admin/workshops/page.tsx
  - src/app/auth/callback/route.ts
  - src/lib/site-url.ts
autonomous: true
requirements:
  - DSEC-01
user_setup: []
must_haves:
  truths:
    - Production-facing application code uses one canonical environment policy for site URL and local-only overrides instead of ad hoc localhost fallbacks
    - `TEST_EMAIL_OVERRIDE` and similar development-only behaviors remain usable locally but are explicitly rejected or ignored in production-sensitive runtime paths
    - Auth redirects, admin-generated links, and email links all derive from the same safe host/origin rules
  artifacts:
    - path: src/lib/env-validator.ts
      provides: Explicit production env safety rules that can be reused by deployment entrypoints and runtime code
      min_lines: 120
    - path: src/lib/site-url.ts
      provides: Canonical helper for resolving public base URL and guarding unsafe localhost fallbacks outside local development
      min_lines: 60
    - path: src/lib/email.ts
      provides: Email link generation and override handling aligned to the canonical environment policy
      min_lines: 180
  key_links:
    - from: src/app/auth/callback/route.ts
      to: src/lib/site-url.ts
      via: callback redirect behavior uses the shared canonical host/origin helper instead of reconstructing production URLs ad hoc
      pattern: safe redirect
    - from: src/app/admin/workshops/page.tsx
      to: src/lib/site-url.ts
      via: workshop link generation stops relying on deprecated `NEXT_PUBLIC_URL` localhost fallback behavior
      pattern: canonical site URL
---

<objective>
Create the runtime environment boundary for Phase 10 by centralizing production-safe URL and override handling in one shared contract.

Purpose: Public deployment currently depends on scattered `process.env` reads and localhost fallbacks. This plan makes production-sensitive runtime behavior explicit before any deployment-oriented build gate is added.
Output: Shared helpers and targeted call-site updates so app/runtime code stops depending on local-only assumptions.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-repository-and-secret-hygiene/10-CONTEXT.md
@.planning/phases/10-repository-and-secret-hygiene/10-RESEARCH.md
@.planning/phases/10-repository-and-secret-hygiene/10-VALIDATION.md
@src/lib/env-validator.ts
@src/lib/email.ts
@src/app/admin/workshops/page.tsx
@src/app/auth/callback/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a canonical site URL and runtime env policy helper</name>
  <files>src/lib/site-url.ts, src/lib/env-validator.ts</files>
  <action>Add a shared server-side helper for canonical public URL resolution and production/local environment policy, then refactor env validation so production-only safety rules are reusable by both runtime code and deployment checks. Keep local maintenance flexibility, but make production-safe behavior the default contract.</action>
  <verify>npm run lint</verify>
  <done>The app has one authoritative place to answer "what is the public site URL" and "is this override allowed in this environment?"</done>
</task>

<task type="auto">
  <name>Task 2: Update runtime call sites to remove local-only fallback behavior</name>
  <files>src/lib/email.ts, src/app/admin/workshops/page.tsx, src/app/auth/callback/route.ts, src/lib/site-url.ts</files>
  <action>Refactor email link generation, workshop admin link generation, and auth callback redirect behavior to use the new canonical helper instead of direct `process.env` reads or localhost fallbacks. Preserve local development ergonomics while making production behavior explicit and safe.</action>
  <verify>npm run test:ci && npm run build</verify>
  <done>All user-visible links and redirects covered by this plan follow the same deployment-safe environment rules.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes
- [ ] `npm run test:ci && npm run build` passes
- [ ] Production-mode checks fail when `NEXT_PUBLIC_SITE_URL` is invalid, missing, or localhost
- [ ] Production-mode checks fail when `TEST_EMAIL_OVERRIDE` is still enabled
</verification>

<success_criteria>

- All tasks completed
- Runtime code no longer depends on deprecated `NEXT_PUBLIC_URL` or ad hoc localhost fallbacks for public links
- Canonical host/origin rules are shared across runtime call sites
- Local-only override behavior is explicitly gated away from production deployment paths
</success_criteria>

<output>
After completion, create `.planning/phases/10-repository-and-secret-hygiene/10-01-SUMMARY.md`
</output>
