---
phase: 10-repository-and-secret-hygiene
plan: "03"
type: execute
wave: 2
depends_on:
  - 10-01
  - 10-02
files_modified:
  - package.json
  - scripts/check-deploy-env.mjs
  - src/lib/env-validator.ts
  - README.md
  - docs/deployment.md
autonomous: true
requirements:
  - DSEC-01
  - DSEC-02
user_setup: []
must_haves:
  truths:
    - Deployment uses an explicit preflight command that validates production-safe environment configuration before or alongside `next build`
    - The deployment contract distinguishes local helper scripts from production app/runtime rules
    - Operators have a concrete Vercel/CI-oriented checklist for env setup and clean repo verification before go-live
  artifacts:
    - path: scripts/check-deploy-env.mjs
      provides: Deployment preflight command for required env vars and unsafe production overrides
      min_lines: 80
    - path: package.json
      provides: Stable script entrypoints that make deployment-safe validation discoverable and reusable
      min_lines: 20
    - path: docs/deployment.md
      provides: Deployment-facing instructions tying repo hygiene and env validation into one operator workflow
      min_lines: 80
  key_links:
    - from: package.json
      to: scripts/check-deploy-env.mjs
      via: build/deploy workflows invoke the same production preflight command
      pattern: deploy preflight
    - from: docs/deployment.md
      to: scripts/check-repo-hygiene.mjs
      via: operator checklist includes both repo hygiene and env validation before build/deploy
      pattern: go-live prerequisite
---

<objective>
Finish Phase 10 by adding the deploy-readiness gate that CI and Vercel-oriented workflows can actually use.

Purpose: Even after runtime env cleanup and repo hygiene hardening, the team still needs one explicit command and one clear document that prove deployment prerequisites are satisfied before public rollout.
Output: A deployment preflight script, package entrypoints, and deployment documentation that convert Phase 10 policy into an executable operator contract.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-repository-and-secret-hygiene/10-CONTEXT.md
@.planning/phases/10-repository-and-secret-hygiene/10-RESEARCH.md
@.planning/phases/10-repository-and-secret-hygiene/10-VALIDATION.md
@.planning/phases/10-repository-and-secret-hygiene/10-01-PLAN.md
@.planning/phases/10-repository-and-secret-hygiene/10-02-PLAN.md
@package.json
@src/lib/env-validator.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a deployment preflight command for production env safety</name>
  <files>scripts/check-deploy-env.mjs, package.json, src/lib/env-validator.ts</files>
  <action>Create a deployment-oriented preflight command that validates required production env vars, canonical site URL safety, and forbidden overrides such as `TEST_EMAIL_OVERRIDE`. Reuse the shared env policy from Plan 01 instead of duplicating rules, and expose it through a stable npm script suitable for PowerShell, CI, and Vercel build hooks.</action>
  <verify>npm run check:deploy-env</verify>
  <done>Deployment readiness can be checked explicitly before public build/deploy starts.</done>
</task>

<task type="auto">
  <name>Task 2: Document the clean deployment workflow end to end</name>
  <files>docs/deployment.md, README.md, package.json</files>
  <action>Write deployment-facing instructions covering repo hygiene, required env variables, preflight commands, and the expected order of operations for clean CI/Vercel builds. Keep the scope to Phase 10 readiness gates; defer Phase 11 public-surface hardening details to the next phase.</action>
  <verify>npm run test:ci && npm run build</verify>
  <done>The team has one concrete deployment workflow that connects repo hygiene, env validation, and build verification.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run check:deploy-env` passes with a valid production-style env configuration
- [ ] `npm run test:ci && npm run build` passes
- [ ] Deployment docs show repo hygiene check before deploy preflight before build
- [ ] The documented workflow is usable from a fresh clone and does not depend on uncommitted local files
</verification>

<success_criteria>

- All tasks completed
- CI/Vercel-oriented workflows have an explicit deployment preflight command
- Repo hygiene and env validation are documented as one deploy-readiness contract
- Phase 10 leaves a concrete operator workflow behind instead of informal setup knowledge
</success_criteria>

<output>
After completion, create `.planning/phases/10-repository-and-secret-hygiene/10-03-SUMMARY.md`
</output>
