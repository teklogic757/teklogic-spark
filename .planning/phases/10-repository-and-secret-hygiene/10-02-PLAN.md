---
phase: 10-repository-and-secret-hygiene
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - .env.example
  - package.json
  - scripts/check-repo-hygiene.mjs
  - README.md
autonomous: true
requirements:
  - DSEC-02
user_setup: []
must_haves:
  truths:
    - The repository has an explicit, repeatable check for tracked secrets, local caches, generated artifacts, and local agent tooling
    - Ignore rules match the actual local artifacts the team generates on Windows and during Next/Vercel workflows
    - Contributors can verify repo hygiene through a stable npm command instead of relying on memory or manual status inspection
  artifacts:
    - path: .gitignore
      provides: Updated ignore rules that cover local-only files and generated state relevant to this repo
      min_lines: 30
    - path: scripts/check-repo-hygiene.mjs
      provides: Scriptable repo audit that fails when tracked or present files violate the deployment hygiene contract
      min_lines: 80
    - path: package.json
      provides: Stable npm entrypoint for repo hygiene checks on Windows, CI, and Vercel-oriented workflows
      min_lines: 20
  key_links:
    - from: package.json
      to: scripts/check-repo-hygiene.mjs
      via: developers and CI use the same repo hygiene command
      pattern: npm script
    - from: scripts/check-repo-hygiene.mjs
      to: .gitignore
      via: script checks and ignore rules enforce the same hygiene boundary
      pattern: tracked artifact guard
---

<objective>
Create the repository hygiene contract for Phase 10 by making secret and artifact safety enforceable from a clean-clone perspective.

Purpose: The repo already ignores many risky paths, but that safety is informal. This plan turns it into a repeatable check so deployment preparation does not depend on manual inspection.
Output: Updated ignore/documentation coverage plus an npm-driven repo hygiene audit.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-repository-and-secret-hygiene/10-CONTEXT.md
@.planning/phases/10-repository-and-secret-hygiene/10-RESEARCH.md
@.planning/phases/10-repository-and-secret-hygiene/10-VALIDATION.md
@.gitignore
@.env.example
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden ignore rules and document the local-only file boundary</name>
  <files>.gitignore, .env.example, README.md</files>
  <action>Review the current ignore list against the actual local artifacts in this repo, then tighten the boundary for secrets, caches, generated output, and local agent tooling. Keep `.env.example` as the committed template, and update docs so contributors understand which files must never be tracked.</action>
  <verify>npm run lint</verify>
  <done>The repository's local-only boundary is documented and aligned with the ignore rules the team actually relies on.</done>
</task>

<task type="auto">
  <name>Task 2: Add an automated repo hygiene audit command</name>
  <files>scripts/check-repo-hygiene.mjs, package.json, .gitignore</files>
  <action>Create a PowerShell-safe npm command that checks for tracked secrets and other forbidden artifacts such as `.env`, `.env.local`, local agent folders, and generated build caches. The command should fail loudly with actionable output so it can be reused in CI and before deployment.</action>
  <verify>npm run check:repo-hygiene</verify>
  <done>Repo hygiene is enforceable by script and no longer depends on manual status inspection.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes
- [ ] `npm run check:repo-hygiene` passes
- [ ] The hygiene script fails if `.env`, `.env.local`, `.vercel`, `.claude`, `.codex`, or generated caches become tracked
- [ ] A fresh clone can follow the documented setup path using `.env.example` without inheriting local-only files
</verification>

<success_criteria>

- All tasks completed
- Repo ignore rules and documentation describe a safe clean-clone boundary
- An automated script enforces the tracked-artifact policy
- The repo is safer for CI and Vercel-oriented workflows
</success_criteria>

<output>
After completion, create `.planning/phases/10-repository-and-secret-hygiene/10-02-SUMMARY.md`
</output>
