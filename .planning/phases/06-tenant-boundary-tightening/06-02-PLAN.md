---
phase: 06-tenant-boundary-tightening
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/lib/training-videos.ts
  - src/lib/dashboard-access.ts
  - src/app/[client_id]/dashboard/page.tsx
  - src/lib/types/database.types.ts
autonomous: true
requirements:
  - SECU-02
user_setup: []
must_haves:
  truths:
    - Training-video reads flow through one reusable helper that accepts the active organization context instead of querying the table ad hoc from the dashboard
    - The training-video read helper encodes a future-safe scope rule that can support global rows and org-specific rows without widening access later
    - The helper's types describe the scoped read contract so future schema expansion does not require another round of broad page-level casts
  artifacts:
    - path: src/lib/training-videos.ts
      provides: Scoped training-video read contract and mapping helpers
      min_lines: 90
    - path: src/lib/dashboard-access.ts
      provides: Dashboard integration point for scoped training-video loading
      min_lines: 90
    - path: src/app/[client_id]/dashboard/page.tsx
      provides: Dashboard page consuming the new training-video access helper
      min_lines: 100
  key_links:
    - from: src/lib/dashboard-access.ts
      to: src/lib/training-videos.ts
      via: dashboard uses one future-safe training-video query path
      pattern: getTrainingVideos
---

<objective>
Replace the dashboard's ad hoc training-video query with a reusable scope-aware contract.

Purpose: The dashboard currently reads training videos directly with service-role access and no tenant-aware abstraction. This plan centralizes that logic so the current global-video behavior remains intact while the code is ready for future org-specific rows without accidental cross-tenant reads.
Output: A scoped training-video helper plus dashboard integration through the new dashboard access layer.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tenant-boundary-tightening/06-VALIDATION.md
@src/lib/training-videos.ts
@src/app/[client_id]/dashboard/page.tsx
@src/lib/types/database.types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a future-safe training-video scope helper</name>
  <files>src/lib/training-videos.ts, src/lib/types/database.types.ts</files>
  <action>Extend the training-video utility layer with a read helper that accepts an organization ID and returns only rows that are globally visible or explicitly tied to that organization. If the current schema has no org-specific field yet, encode the contract so the selection logic is centralized and future expansion only changes the helper, not each caller.</action>
  <verify>npm run lint</verify>
  <done>Training-video reads have one reusable scoping contract instead of page-local ad hoc queries.</done>
</task>

<task type="auto">
  <name>Task 2: Route dashboard training resources through the scoped helper</name>
  <files>src/lib/dashboard-access.ts, src/app/[client_id]/dashboard/page.tsx, src/lib/training-videos.ts</files>
  <action>Move the dashboard's training-video loading behind the new scoped helper and ensure the page only receives mapped list items, not raw query output. Preserve the current fallback static dataset behavior when no rows are returned, but keep the tenant-boundary rule centralized in one place.</action>
  <verify>npm run build</verify>
  <done>The dashboard no longer queries training videos directly or owns the scoping logic.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes
- [ ] `npm run build` passes
- [ ] Manual data check confirms only global rows plus the current tenant's rows are returned when org-specific videos exist
</verification>

<success_criteria>

- All tasks completed
- Training-video reads have a reusable tenant-safe contract
- Dashboard no longer owns training-video scoping logic
- Future schema changes can stay isolated to one helper
</success_criteria>

<output>
After completion, create `.planning/phases/06-tenant-boundary-tightening/06-02-SUMMARY.md`
</output>
