---
phase: 06-tenant-boundary-tightening
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/lib/leaderboard.ts
  - src/app/[client_id]/leaderboard/page.tsx
  - src/app/[client_id]/m/page.tsx
  - src/app/[client_id]/m/leaderboard/page.tsx
  - src/app/[client_id]/dashboard/page.tsx
autonomous: true
requirements:
  - SECU-01
  - SECU-03
user_setup: []
must_haves:
  truths:
    - Score and leaderboard reads on tenant-facing pages stop defaulting to service-role clients when a granted user-scoped read model is sufficient
    - Shared leaderboard access helpers can use a user-scoped client for tenant-facing views while preserving tenant filters explicitly in code
    - Tenant-facing pages that consume leaderboard data also shed avoidable casted result shapes in favor of typed helper returns
  artifacts:
    - path: src/lib/leaderboard.ts
      provides: Tenant-facing leaderboard helpers that no longer hardcode service-role access
      min_lines: 70
    - path: src/app/[client_id]/leaderboard/page.tsx
      provides: Full leaderboard page consuming the narrowed helper contract
      min_lines: 80
    - path: src/app/[client_id]/m/leaderboard/page.tsx
      provides: Mobile leaderboard page using the same narrowed helper contract
      min_lines: 80
  key_links:
    - from: src/app/[client_id]/leaderboard/page.tsx
      to: src/lib/leaderboard.ts
      via: tenant leaderboard reads no longer require broad privileged access
      pattern: getOrganizationLeaderboard
    - from: src/app/[client_id]/m/leaderboard/page.tsx
      to: src/lib/leaderboard.ts
      via: mobile rankings share the same narrowed contract
      pattern: getOrganizationLeaderboard
---

<objective>
Finish narrowing tenant-facing privileged reads by removing avoidable service-role access from leaderboard-driven UI.

Purpose: Phase 5 centralized leaderboard reads, but the shared helper still hardcodes `createAdminClient()`. This plan converts that tenant-facing read path to the least-privilege client that already has granted access, then updates the consuming pages to rely on the typed helper contract.
Output: A narrowed leaderboard helper and tenant-facing pages that consume typed tenant-safe leaderboard data without broad privileged reads.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tenant-boundary-tightening/06-VALIDATION.md
@src/lib/leaderboard.ts
@src/app/[client_id]/leaderboard/page.tsx
@src/app/[client_id]/m/page.tsx
@src/app/[client_id]/m/leaderboard/page.tsx
@src/app/[client_id]/dashboard/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Narrow tenant-facing leaderboard helpers to the least-privilege client</name>
  <files>src/lib/leaderboard.ts, src/app/[client_id]/dashboard/page.tsx, src/app/[client_id]/leaderboard/page.tsx, src/app/[client_id]/m/page.tsx, src/app/[client_id]/m/leaderboard/page.tsx</files>
  <action>Refactor the shared leaderboard helpers so tenant-facing routes can pass a user-scoped Supabase client instead of always creating a service-role client internally. Keep tenant filters explicit and preserve a clear boundary if any truly privileged leaderboard-style reads remain elsewhere.</action>
  <verify>npm run lint</verify>
  <done>Tenant-facing leaderboard reads no longer default to service-role access.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten typed consumption of leaderboard data across tenant pages</name>
  <files>src/lib/leaderboard.ts, src/app/[client_id]/leaderboard/page.tsx, src/app/[client_id]/m/page.tsx, src/app/[client_id]/m/leaderboard/page.tsx, src/app/[client_id]/dashboard/page.tsx</files>
  <action>Update the dashboard, full leaderboard, and mobile leaderboard consumers so they pass the appropriate scoped client into the helper and receive typed entries without page-local shape coercion. Preserve current ranking behavior and redirects, but leave the helper contract explicit enough that future tenant policies can be audited from one place.</action>
  <verify>npm run build</verify>
  <done>The tenant-facing leaderboard surfaces consume one typed, least-privilege read contract.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes
- [ ] `npm run build` passes
- [ ] Manual spot-check confirms dashboard, desktop leaderboard, and mobile leaderboard still render correctly for a normal tenant user after the helper switches away from default service-role access
</verification>

<success_criteria>

- All tasks completed
- Avoidable service-role usage is removed from tenant-facing leaderboard reads
- Shared leaderboard access stays explicit and typed
- Tenant-facing pages consume narrowed helper results without broad casts
</success_criteria>

<output>
After completion, create `.planning/phases/06-tenant-boundary-tightening/06-03-SUMMARY.md`
</output>
