---
phase: 09-test-baseline-and-join-cleanup
plan: "03"
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - src/app/join/actions.ts
  - src/app/join/actions.test.ts
  - .planning/phases/09-test-baseline-and-join-cleanup/09-VERIFICATION.md
autonomous: true
requirements:
  - TEST-03
user_setup: []
must_haves:
  truths:
    - The workshop join query no longer contains duplicate filters that hide intent and increase maintenance risk
    - Automated regression coverage verifies join behavior for invalid, throttled, expired, and valid code flows
    - Verification notes clearly document any remaining manual checks tied to live workshop cookies and redirects
  artifacts:
    - path: src/app/join/actions.ts
      provides: simplified workshop join query and preserved redirect behavior
      min_lines: 120
    - path: src/app/join/actions.test.ts
      provides: regression tests covering join edge cases and happy path behavior
      min_lines: 120
    - path: .planning/phases/09-test-baseline-and-join-cleanup/09-VERIFICATION.md
      provides: focused manual verification checklist for live join flow
      min_lines: 60
  key_links:
    - from: src/app/join/actions.test.ts
      to: src/app/join/actions.ts
      via: duplicate-filter cleanup is protected by behavior-focused regression tests
      pattern: joinWorkshop
---

<objective>
Remove the workshop join logic smell and lock in behavior with targeted regression coverage.

Purpose: The duplicate `code` filter in `joinWorkshop` is a known smell. This plan cleans that path and verifies intended behavior stays intact through tests and focused verification docs.
Output: Cleaned join query logic plus automated and manual verification coverage for workshop join flow.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-test-baseline-and-join-cleanup/09-VALIDATION.md
@src/app/join/actions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove duplicate workshop join filter and preserve behavior</name>
  <files>src/app/join/actions.ts</files>
  <action>Refactor `joinWorkshop` to remove the duplicated `.eq('code', ...)` constraint while preserving existing rate-limit, expiry validation, cookie set, and redirect behavior. Keep the change narrowly scoped and avoid unrelated auth or routing changes.</action>
  <verify>npm run test -- --run src/app/join/actions.test.ts</verify>
  <done>The join query smell is removed without changing intended behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Add join regression tests and focused verification notes</name>
  <files>src/app/join/actions.test.ts, .planning/phases/09-test-baseline-and-join-cleanup/09-VERIFICATION.md</files>
  <action>Add tests for invalid format, invalid code, inactive/expired code, throttled requests, and valid redirect flow. Write `09-VERIFICATION.md` with manual checks for cookie/redirect behavior in a real environment where automated tests cannot fully simulate browser + Supabase session behavior.</action>
  <verify>npm run test:ci && npm run build</verify>
  <done>TEST-03 has both automated regression coverage and explicit manual verification guidance.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test -- --run src/app/join/actions.test.ts` passes
- [ ] `npm run test:ci` passes
- [ ] `npm run build` passes with existing env policy expectations documented
- [ ] `09-VERIFICATION.md` exists and includes explicit manual workshop join checks
</verification>

<success_criteria>

- All tasks completed
- TEST-03 duplicate join filter smell is removed
- Workshop join behavior is covered by regression tests and focused verification docs
</success_criteria>

<output>
After completion, create `.planning/phases/09-test-baseline-and-join-cleanup/09-03-SUMMARY.md`
</output>
