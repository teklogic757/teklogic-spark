---
phase: 09-test-baseline-and-join-cleanup
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/lib/score.ts
  - src/lib/dashboard-access.ts
  - src/lib/submit-flow.ts
  - src/test/mocks/supabase.ts
  - src/lib/score.test.ts
  - src/lib/dashboard-access.test.ts
  - src/lib/submit-flow.test.ts
autonomous: true
requirements:
  - TEST-02
user_setup: []
must_haves:
  truths:
    - Canonical score math has direct automated coverage to prevent weighted scoring regressions
    - Tenant-safe dashboard access behavior is covered with explicit tests for auth/profile/org mismatch boundaries
    - Submission flow tests prove persistence-critical success/failure boundaries are preserved when side effects fail
  artifacts:
    - path: src/lib/score.test.ts
      provides: coverage for canonical weighted scoring behavior and mismatch handling
      min_lines: 80
    - path: src/lib/dashboard-access.test.ts
      provides: coverage for tenant boundary decisions and redirect reasoning
      min_lines: 120
    - path: src/lib/submit-flow.test.ts
      provides: coverage for critical submit success and failure boundaries
      min_lines: 140
    - path: src/test/mocks/supabase.ts
      provides: reusable Supabase mocking helpers to keep tests maintainable
      min_lines: 80
  key_links:
    - from: src/lib/score.test.ts
      to: src/lib/score.ts
      via: weighted rubric score logic remains deterministic under refactors
      pattern: computeCanonicalWeightedScore
    - from: src/lib/dashboard-access.test.ts
      to: src/lib/dashboard-access.ts
      via: tenant-safe routing and access boundary behavior stays explicit
      pattern: getTenantRequestContext
    - from: src/lib/submit-flow.test.ts
      to: src/lib/submit-flow.ts
      via: submission success/failure boundaries are protected from side-effect regressions
      pattern: submitIdeaFlow
---

<objective>
Add regression coverage for the highest-risk trust boundaries introduced in milestone v1.1.

Purpose: Phase 9 needs meaningful tests, not just framework setup. This plan anchors automated coverage to score integrity, tenant-safe dashboard access, and submission-path correctness.
Output: Focused automated tests for canonical scoring, dashboard access boundaries, and submission flow behavior.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-test-baseline-and-join-cleanup/09-VALIDATION.md
@src/lib/score.ts
@src/lib/dashboard-access.ts
@src/lib/submit-flow.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add canonical scoring and dashboard tenant-boundary test coverage</name>
  <files>src/lib/score.test.ts, src/lib/dashboard-access.test.ts, src/test/mocks/supabase.ts</files>
  <action>Create deterministic unit tests for weighted score normalization and mismatch handling in `score.ts`, then add boundary-focused tests for `getTenantRequestContext` (missing user, missing profile, missing org, wrong tenant redirect, valid tenant). Reuse a shared Supabase mock helper to avoid brittle one-off test doubles.</action>
  <verify>npm run test -- --run src/lib/score.test.ts src/lib/dashboard-access.test.ts</verify>
  <done>Score and dashboard boundary regressions are automatically caught by tests.</done>
</task>

<task type="auto">
  <name>Task 2: Add critical submission success/failure boundary tests</name>
  <files>src/lib/submit-flow.test.ts, src/test/mocks/supabase.ts</files>
  <action>Add tests around submit flow branches that must remain stable: authenticated success path, guest/workshop guard behavior, persistence failure handling, and non-critical side-effect failure that should not drop a persisted idea. Use targeted mocks and keep assertions focused on boundary outcomes instead of implementation internals.</action>
  <verify>npm run test:ci</verify>
  <done>Critical submit flow boundaries have baseline automated coverage tied to TEST-02.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test -- --run src/lib/score.test.ts src/lib/dashboard-access.test.ts src/lib/submit-flow.test.ts` passes
- [ ] `npm run test:ci` passes
- [ ] `npm run build` still succeeds after test additions
</verification>

<success_criteria>

- All tasks completed
- TEST-02 coverage exists for score canonicalization, tenant-safe dashboard access, and submission success/failure boundaries
- New tests are deterministic and executable through committed package scripts
</success_criteria>

<output>
After completion, create `.planning/phases/09-test-baseline-and-join-cleanup/09-02-SUMMARY.md`
</output>
