---
phase: 09-test-baseline-and-join-cleanup
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - vitest.config.ts
  - src/test/setup.ts
  - .planning/phases/09-test-baseline-and-join-cleanup/09-VALIDATION.md
autonomous: true
requirements:
  - TEST-01
user_setup: []
must_haves:
  truths:
    - The repository has a first-class automated test runner available from package scripts, not ad hoc local commands
    - Test scripts run in both local PowerShell and CI-style non-watch mode
    - The new test tooling coexists with current lint/build flows without breaking existing developer commands
  artifacts:
    - path: package.json
      provides: test scripts for local and CI execution
      min_lines: 20
    - path: vitest.config.ts
      provides: deterministic test runner configuration suitable for server-first modules
      min_lines: 20
    - path: src/test/setup.ts
      provides: shared test setup for consistent mocks and environment
      min_lines: 10
  key_links:
    - from: package.json
      to: vitest.config.ts
      via: package scripts invoke the committed test configuration
      pattern: test
---

<objective>
Establish the baseline automated test infrastructure required by Phase 9.

Purpose: Phase 9 cannot validate behavior regressions until the repo has a stable, documented test runner that works in local development and CI style execution.
Output: Vitest-based test tooling, package scripts, and setup scaffolding ready for targeted coverage in later plans.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-test-baseline-and-join-cleanup/09-VALIDATION.md
@package.json
@tsconfig.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install and configure baseline test tooling</name>
  <files>package.json, vitest.config.ts, src/test/setup.ts, tsconfig.json</files>
  <action>Add Vitest and required companion packages, wire scripts (`test`, `test:watch`, `test:ci`), and create baseline config/setup files tuned for Node/server module testing used by this repo. Ensure commands run in non-watch mode for CI and keep watch mode explicitly optional.</action>
  <verify>npm run test -- --run</verify>
  <done>The project can execute automated tests using a committed script and config.</done>
</task>

<task type="auto">
  <name>Task 2: Document the test entrypoint contract for downstream plans</name>
  <files>package.json, .planning/phases/09-test-baseline-and-join-cleanup/09-VALIDATION.md</files>
  <action>Confirm the validation strategy commands match real scripts and keep the verification contract explicit for Phase 9 execution. Update only the phase validation file where needed so later plans can rely on the same commands.</action>
  <verify>npm run test:ci</verify>
  <done>Phase 9 has a stable test command contract usable by plan execution and verification.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test -- --run` passes
- [ ] `npm run test:ci` runs in non-watch mode and exits cleanly
- [ ] Existing `npm run lint` remains functional after test tooling changes
</verification>

<success_criteria>

- All tasks completed
- TEST-01 baseline test runner requirement is satisfied
- Test scripts are committed and reproducible in local and CI workflows
</success_criteria>

<output>
After completion, create `.planning/phases/09-test-baseline-and-join-cleanup/09-01-SUMMARY.md`
</output>
